var createTypes = function createTypes(models) {
  return Object.keys(models).reduce(function (accumulator, key) {
    if (models[key].types) {
      accumulator[key] = models[key].types;
    }

    return accumulator;
  }, {});
};

export default createTypes;