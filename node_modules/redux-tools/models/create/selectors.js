var createSelectors = function createSelectors(models) {
  return Object.keys(models).reduce(function (accumulator, key) {
    if (models[key].selectors) {
      accumulator[key] = models[key].selectors;
    }

    return accumulator;
  }, {});
};

export default createSelectors;