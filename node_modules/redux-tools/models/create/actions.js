var createActions = function createActions(models) {
  return Object.keys(models).reduce(function (accumulator, key) {
    if (models[key].actions) {
      accumulator[key] = models[key].actions;
    }

    return accumulator;
  }, {});
};

export default createActions;