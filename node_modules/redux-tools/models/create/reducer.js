import { combineReducers } from 'redux';

var createReducer = function createReducer(models) {
  return combineReducers(Object.keys(models).reduce(function (accumulator, key) {
    if (models[key].reducer) {
      accumulator[key] = models[key].reducer;
    }

    return accumulator;
  }, {}));
};

export default createReducer;