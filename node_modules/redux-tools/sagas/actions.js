import types from './types';
var actions = {
  safelyFailure: function safelyFailure(payload) {
    return {
      payload: payload,
      type: types.SAFELY_FAILURE
    };
  }
};
export default actions;