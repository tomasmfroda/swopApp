import _objectSpread from "/Users/william/Dropbox/redux-tools/node_modules/@babel/runtime/helpers/esm/objectSpread";

var generateActionsAndTypes = function generateActionsAndTypes(namespace, types) {
  types.forEach(function (type) {
    return type.setNamespace(namespace);
  });
  return {
    actions: types.reduce(function (actions, type) {
      return _objectSpread({}, actions, type.actions());
    }, {}),
    types: types.reduce(function (result, type) {
      return _objectSpread({}, result, type.types());
    }, {})
  };
};

export default generateActionsAndTypes;